<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="16dp"
    app:cardBackgroundColor="@color/card_background"
    app:cardElevation="0dp"
    app:strokeWidth="0dp"
    android:clickable="true"
    android:focusable="true">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Header bar: favicon + title + close -->
        <LinearLayout
            android:id="@+id/tabHeaderBar"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="10dp"
            android:paddingEnd="4dp"
            android:background="@color/card_background">

            <!-- Favicon -->
            <ImageView
                android:id="@+id/tabFavicon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_helix_logo"
                android:scaleType="centerCrop"
                tools:ignore="ContentDescription" />

            <!-- Incognito icon -->
            <ImageView
                android:id="@+id/incognitoIcon"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:src="@drawable/ic_incognito"
                android:visibility="gone"
                tools:ignore="ContentDescription" />

            <!-- Title -->
            <TextView
                android:id="@+id/tabTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:layout_marginEnd="4dp"
                android:textColor="@color/text_primary"
                android:textSize="12sp"
                android:singleLine="true"
                android:ellipsize="end"
                android:fontFamily="sans-serif-medium"
                tools:text="Google" />

            <!-- Close button -->
            <ImageButton
                android:id="@+id/btnCloseTab"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:padding="6dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_close"
                app:tint="@color/text_secondary"
                android:contentDescription="@string/desc_close" />
        </LinearLayout>

        <!-- Active tab indicator (thin line under header) -->
        <View
            android:id="@+id/activeIndicator"
            android:layout_width="match_parent"
            android:layout_height="2dp"
            android:background="@drawable/bg_bottom_nav_indicator"
            android:visibility="gone" />

        <!-- Thumbnail / Preview -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="200dp">

            <ImageView
                android:id="@+id/tabThumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:scaleType="centerCrop"
                android:background="@color/card_background"
                tools:ignore="ContentDescription" />

            <!-- Incognito badge overlay -->
            <TextView
                android:id="@+id/incognitoBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_margin="6dp"
                android:paddingHorizontal="6dp"
                android:paddingVertical="2dp"
                android:background="@drawable/bg_incognito_badge"
                android:text="@string/private_tab_label"
                android:textColor="#FFFFFF"
                android:textSize="8sp"
                android:textStyle="bold"
                android:visibility="gone" />

            <!-- URL overlay at bottom with security icon -->
            <LinearLayout
                android:id="@+id/urlBar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_gravity="bottom"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:background="#80000000"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp">

                <ImageView
                    android:id="@+id/securityIcon"
                    android:layout_width="12dp"
                    android:layout_height="12dp"
                    android:layout_marginEnd="4dp"
                    android:src="@drawable/ic_lock"
                    tools:ignore="ContentDescription" />

                <TextView
                    android:id="@+id/tabUrl"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:textColor="#99FFFFFF"
                    android:textSize="9sp"
                    android:singleLine="true"
                    android:ellipsize="end"
                    tools:text="google.com" />
            </LinearLayout>
        </FrameLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
